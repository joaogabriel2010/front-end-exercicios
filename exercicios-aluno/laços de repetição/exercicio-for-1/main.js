let peso = 0;
let pesoTotal = 0;

for (let cont - 1; cont < 11; cont*= 1)
    peso = prompt('insira o peso da pessoa número ' + cont);
pesoTotal = pesototal + mutationobserverpeso;
console.log(pesototal);